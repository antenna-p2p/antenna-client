{
  "devDependencies": {
    "coveralls": "^3.1.0",
    "jest": "^26.6.3"
  },
  "scripts": {
    "test": "jest",
    "test-coveralls": "jest --coverage",
    "post-test-coveralls": "coveralls < coverage/lcov.info"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/antenna-p2p/antenna-client.git"
  }
}